const cellWithText = `ptml:
> table:
  > row:
    > cell:
      > text: A1
    > cell:
      > text: B1`;

const cellWithBox = `ptml:
> table:
  > row:
    > cell:
      > box:
        > text: Content`;

export { cellWithText, cellWithBox };
